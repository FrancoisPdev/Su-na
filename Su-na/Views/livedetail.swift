//
//  livedetail.swift
//  Su-na
//
//  Created by Schmidt Samuelk on 11/05/2020.
//  Copyright © 2020 PARENT François. All rights reserved.
//

/*
import UIKit
import AVKit
import AVFoundation
import SwiftUI

struct LiveDetail: View {
    var body: some View {
        
        // MARK: variables
        var hauteurMenuBar = NSApplication.shared.mainMenu?.menuBarHeight
        var urlVideo = URL(string: "/Users/francisdolbec/Movies/Séries Télé/Rick et Morty/Rick.and.Morty.S01E01.VFQ.HDTV.1080p.x264-Kamek.mp4")
        let player = AVPlayer(url: urlVideo!)


        struct ContentView : View {
            var body: some View {

                VStack {
                    Text("Hello World")
                        .frame(maxWidth: .infinity, maxHeight: .infinity)
                    Text("PATATE!")
                    //player.play()
                    }

                .frame(minWidth: 1024, idealWidth: 1440, maxWidth: .infinity, minHeight: (640-hauteurMenuBar!), idealHeight: (900-hauteurMenuBar!), maxHeight: .infinity)

            }
        }


        #if DEBUG
        struct ContentView_Previews : PreviewProvider {
            static var previews: some View {
                ContentView()
            }
        }
        #endif*/
